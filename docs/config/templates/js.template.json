{
  "name": "{{ doc.codeName | linebreak }}",
  "source": "{{ doc.outputPath }}",
  "description": "{{doc.description | linebreak}}",
  {% if doc.params %}
  "params": [
    {% for param in doc.params %}
      {
        "name": "{{param.name}}",
        "description": "{{param.description | linebreak}}",
        "typeList": "{{param.typeList}}"
      }{% if loop.last != true %},{% endif %}
    {% endfor %}
  ],
  {% endif %}
  "returns": {
    "typeList": "{{doc.returns.typeList}}",
    "description": "{{doc.returns.description | linebreak}}"
  },
  "example": "{{doc.example}}"
}